'use strict';
const $ = id => document.getElementById(id);
const kinds = {Q:'Questions', R:'Research items', H:'Hypotheses', T:'Theories'};
let snapshot = {records:[], tags:[]}, filter = 'all', tag = '', token = '', requestKey = '';
function element(name, className, text) { const e=document.createElement(name); if(className)e.className=className; if(text!==undefined)e.textContent=text; return e; }
async function api(path, data) { const options=data ? {method:'POST',headers:{'Content-Type':'application/json','X-Research-Token':token},body:JSON.stringify(data)} : {}; const response=await fetch(path,options); const result=await response.json(); if(!response.ok)throw Error(result.error || 'Request failed'); return result; }
async function refresh(){ try{snapshot=await api('/api/corpus');$('notice').hidden=true;render();}catch(e){$('notice').textContent=e.message;$('notice').hidden=false;} }
function render(){
 $('stats').replaceChildren(...Object.entries(kinds).map(([kind,label])=>{const card=element('div','stat');card.append(element('strong','',String(snapshot.records.filter(r=>r.kind===kind).length)),element('span','',label));return card;}));
 $('tags').replaceChildren(...['',...snapshot.tags].map(t=>{const b=element('button','tag'+(tag===t?' selected':''),t||'All tags');b.onclick=()=>{tag=t;render();};return b;}));
 const query=$('search').value.toLowerCase();
 const records=snapshot.records.filter(r=>(filter==='all'||r.kind===filter)&&(!tag||r.metadata.tags.includes(tag))&&[r.id,r.metadata.title,...r.metadata.tags].join(' ').toLowerCase().includes(query));
 $('records').replaceChildren(...records.map(r=>{const row=element('button','record');const main=element('div','record-main');main.append(element('h3','',r.metadata.title),element('p','',r.metadata.tags.join(' · ')||'No tags yet'));row.append(element('span','kind',r.id),main,element('span','status',r.metadata.status));row.onclick=()=>showDetail(r);return row;}));
 if(!records.length)$('records').append(element('div','empty',snapshot.records.length?'No records match this view.':'Start with a question. Your research will take shape here.'));
 $('revision').textContent=`Canonical Markdown · corpus ${snapshot.revision?.slice(0,10)||'unknown'} · no execution implied`;
}
function showDetail(r){$('detail-id').textContent=`${r.id} / ${kinds[r.kind]}`;$('detail-title').textContent=r.metadata.title;$('detail-meta').textContent=`${r.metadata.status} · ${r.metadata.tags.join(' · ')}`;$('detail-body').textContent=r.body;$('detail-lineage').textContent=r.metadata.derived_from.length?`Derived from ${r.metadata.derived_from.join(', ')}`:'No predecessors';$('detail').showModal();}
document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{filter=b.dataset.filter;document.querySelectorAll('[data-filter]').forEach(n=>n.classList.toggle('active',n===b));document.querySelector('h1').textContent=filter==='all'?'Overview':kinds[filter];render();});
$('search').oninput=render;
$('new-question').onclick=()=>{requestKey=crypto.randomUUID();$('capture-form').reset();$('capture-error').textContent='';$('capture').showModal();};
$('close-capture').onclick=()=>$('capture').close();$('close-detail').onclick=()=>$('detail').close();
$('capture-form').onsubmit=async e=>{e.preventDefault();const form=new FormData(e.target);const button=e.target.querySelector('[type=submit]');button.disabled=true;try{await api('/api/questions',{request_key:requestKey,title:form.get('title'),body:form.get('body'),tags:form.get('tags').split(',').map(t=>t.trim()).filter(Boolean)});$('capture').close();await refresh();}catch(error){$('capture-error').textContent=error.message;}finally{button.disabled=false;}};
(async()=>{try{token=(await api('/api/session')).token;await refresh();}catch(e){$('notice').hidden=false;$('notice').textContent=e.message;}})();

let links = [], selected = null, plans = new Map(), requestKeys = new Map();
async function operation(operation, argumentsValue) { return api('/api/operations',{operation,arguments:argumentsValue}); }
async function refreshBackend(){try{await api('/api/backend');$('backend-status').textContent='Orbit backend ready for explicit operator actions.';$('backend-status').className='backend-status ready';}catch(e){$('backend-status').textContent='Local capture is available; execution backend unavailable.';$('backend-status').className='backend-status unavailable';}}
const baseShowDetail = showDetail;
showDetail = function(r){selected=r;baseShowDetail(r);$('work-panel').hidden=r.kind!=='R';if(r.kind==='R')renderWork(r);};
function renderWork(r){const link=links.find(item=>item.research_id===r.id);const form=$('work-form');const stored=plans.get(r.id);if(stored)form.elements.objective.value=stored.objective||'';if(requestKeys.has(r.id))form.elements.requestKey.value=requestKeys.get(r.id);$('work-state').textContent=link?'Task link recorded; enter the request key to read fresh status.':'No linked task. Plan locally, then link it explicitly.';}
function workValues(){const form=new FormData($('work-form'));return {objective:String(form.get('objective')||''),requestKey:String(form.get('requestKey')||''),taskTitle:String(form.get('taskTitle')||''),crew:String(form.get('crew')||''),base:String(form.get('base')||''),receiptPath:String(form.get('receiptPath')||'')};}
async function runWork(action){$('work-error').textContent='';const values=workValues();try{if(!selected)return;if(action==='plan'){const plan=await operation('research.plan_investigation',{research_id:selected.id,objective:values.objective});plans.set(selected.id,{...plan,objective:values.objective});$('work-state').textContent='Plan ready. Linking remains an explicit action.';return;}if(action==='link'){const plan=plans.get(selected.id);if(!plan)throw Error('Plan the investigation before linking a task.');const link=await operation('research.link_work',{request_key:values.requestKey,title:values.taskTitle,crew:values.crew,plan});requestKeys.set(selected.id,values.requestKey);links=links.filter(item=>item.research_id!==selected.id).concat(link);$('work-state').textContent='Task link recorded. Promote or dispatch explicitly.';}if(action==='status'){if(!values.requestKey)throw Error('Enter the request key used when linking the task.');const status=await api(`/api/work-status?request_key=${encodeURIComponent(values.requestKey)}`);$('work-state').textContent=`Execution state: ${status.run?.state||status.task?.status||'linked; no run observed'}. Scientific assessment remains separate.`;}if(action==='promote'){await operation('research.promote',{request_key:values.requestKey});$('work-state').textContent='Task promoted. Dispatch is still a separate operator action.';}if(action==='dispatch'){if(!values.base)throw Error('Enter the explicit dispatch base.');await operation('research.dispatch',{request_key:values.requestKey,base:values.base});$('work-state').textContent='Dispatch submitted; await a receipt before calling the result accepted.';}if(action==='cancel'){await operation('research.cancel',{request_key:values.requestKey});$('work-state').textContent='Run cancellation requested.';}if(action==='validate'){if(!values.receiptPath)throw Error('Enter the committed receipt path.');await operation('research.validate_result',{request_key:values.requestKey,receipt_path:values.receiptPath});$('work-state').textContent='Published result accepted by the receipt gate; this does not create a scientific assessment.';}await refresh();}catch(error){$('work-error').textContent=error.message;}}
[['plan-work','plan'],['link-work','link'],['status-work','status'],['promote-work','promote'],['dispatch-work','dispatch'],['cancel-work','cancel']].forEach(([id,action])=>$(id).onclick=()=>runWork(action));
const receiptInput=element('input','');receiptInput.name='receiptPath';receiptInput.maxLength=500;receiptInput.placeholder='Committed receipt evidence path';const receiptLabel=element('label','');receiptLabel.textContent='Receipt path';receiptLabel.append(receiptInput);$('work-form').insertBefore(receiptLabel,$('work-form').querySelector('.work-actions'));const validateButton=element('button','', 'Validate result');validateButton.type='button';validateButton.onclick=()=>runWork('validate');$('work-form').querySelector('.work-actions').append(validateButton);
const baseRefresh = refresh;
refresh = async function(){try{snapshot=await api('/api/corpus');links=await api('/api/work-links');$('notice').hidden=true;render();if(selected)showDetail(selected);}catch(e){$('notice').textContent=e.message;$('notice').hidden=false;}};
refreshBackend();
